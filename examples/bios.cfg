MEMORY {
  DATA: start = $0000, size = $0600, type = rw, fill = yes;
  ROM: start = $0600, size = $f9fa, type = ro, fill = yes;
  RESETVEC: start = $fffa, size = 6, type = ro, fill = yes;
}

SEGMENTS {
  DATA: load = "DATA", type = rw;
  CODE: load = "ROM", type = ro;
  RESETVEC: load = "RESETVEC", type = ro;
}
